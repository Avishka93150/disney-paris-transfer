---
# cPanel "Git Version Control" deployment hook.
#
# Present at the root of the `deploy` branch (copied there by the GitHub
# workflow). When the cPanel user clicks "Deploy HEAD Commit", cPanel runs the
# tasks below inside the cloned repository, which is also the Node.js
# application's root.
deployment:
  tasks:
    - /bin/bash ./deploy.sh
